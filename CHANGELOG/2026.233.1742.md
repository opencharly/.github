# 2026.233.1742 — Rename-then-validate flow
<!-- auto-merge-finalized: 2026.233.1742 -->

The pr-validator now renames the CHANGELOG placeholder FIRST, then
exits (the push triggers a fresh run that validates ONCE). The AI
review runs once per PR. Removes the broken approve-validator-runs
workflow (the workflow_run event doesn't fire for action_required
runs).

Change class: CI/workflow — flow simplification.
