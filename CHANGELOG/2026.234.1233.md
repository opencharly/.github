# 2026.234.1233 — Bump pi-review-action to v1.0.10; tag-on-merge uses PR body as changelog

## Change

- Bump `opencharly/pi-review-action` from v1.0.9 to v1.0.10 — the new
  prompt drops the CHANGELOG-file requirement (the PR body IS the changelog).
- `tag-on-merge.yml`: create `CHANGELOG/<calver>.md` from the merged PR's
  title + body (no `## CHANGELOG` section extraction; no diff file required).
