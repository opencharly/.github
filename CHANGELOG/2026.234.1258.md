# 2026.234.1258 — Fix CHANGELOG creation on protected main

## Change

- `tag-on-merge.yml`: the CHANGELOG Contents-API write to `main` failed
  with GITHUB_TOKEN because `main` is branch-protected (required status
  checks `verify` + `validate / validate`). Fix: create a bot token
  (GitHub App preferred, PAT fallback — the same pattern the org-wide
  pr-validator uses) and authorize the Contents write with it.
