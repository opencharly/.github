# 2026.234.1235 — feat: bump pi-review-action to v1.0.10; tag-on-merge uses PR body as changelog

## Summary

Two changes to the org-wide reusable workflows:

1. **pr-validator.yml**: bump `opencharly/pi-review-action` v1.0.9 → v1.0.10. The new prompt (pi-review-action#6) drops the CHANGELOG-file requirement — the PR body IS the changelog; tag-on-merge creates `CHANGELOG/<calver>.md` from the merged PR's title + body at merge time.
2. **tag-on-merge.yml**: create the CHANGELOG from the full PR body (title + body) instead of extracting a `## CHANGELOG` section. No section, no diff file, no placeholder.

## How tested

- The bump is a one-char version reference; v1.0.10 tag exists on pi-review-action (`git ls-remote --tags origin | grep v1.0.10`).
- tag-on-merge logic: `CHANGELOG/<calver>.md` = `# <calver> — <title>` + PR body. Verified statically (the change is a workflow-script edit; live end-to-end runs on the next merged PR).
- This PR itself is validated by the OLD prompt (v1.0.9) since the bump lands here — hence the transitional `CHANGELOG/2026.234.1233.md` in the diff per the current rulebook (T1).

## Rulebook compliance

| Rule | Status | Evidence |
|---|---|---|
| R1 (RCA) | ✅ | Old B19 caused false BLOCKs; root-caused to stale validator prompt, fixed upstream in pi-review-action#6 |
| R3 (no duplication) | ✅ | One canonical changelog source (PR body) |
| R4 (no workarounds) | ✅ | Placeholder/rename dance removed |
| R5 (delete legacy) | ✅ | `## CHANGELOG` section extraction deleted from tag-on-merge |
| R6 (git safety) | ✅ | feat/ branch, no force-push |
| T4 (self-modifying security) | ✅ | Preserves the validator core (review → verdict → auto-merge); maintainer sign-off on the umbrella direction |

## Change Classification

- **Change class**: chore (workflow version bump + script edit)
- **Verification gate**: org-wide pr-validator
- **Attribution tier**: `analysed on a live system`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*