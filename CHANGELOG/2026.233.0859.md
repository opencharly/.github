# 2026.233.0859 — Add workflow_dispatch trigger to dispatcher template

Adds workflow_dispatch as a manual trigger option for the auto-merge
dispatcher, alongside the existing check_run and workflow_run triggers.
This enables manual triggering when event-based triggers are unreliable.

Change class: CI/workflow — trigger configuration change.
