# 2026.233.0859 — Add workflow_dispatch trigger and fix guard conditions

Adds `workflow_dispatch` trigger alongside the existing `check_run`
trigger.  Fixes the `workflow_run` trigger to check `conclusion ==
'success'`.  The `workflow_dispatch` input `head-sha` is required.

Change class: CI/workflow — trigger configuration change.
