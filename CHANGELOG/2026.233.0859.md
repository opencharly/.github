# 2026.233.0859 — Simplify triggers: workflow_run and check_run only

Removes the `workflow_dispatch` trigger from the dispatcher.
Keeps `workflow_run` and `check_run` triggers with branch guards.

Change class: CI/workflow — trigger configuration change.
