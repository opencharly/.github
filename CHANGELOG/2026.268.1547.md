# 2026.268.1547 — docs(validator): sweep the stale header — actual pin, no fallback, engine-default HIGH

## Summary

Sweep three stale claims out of the `pr-validator.yml` header (R1/R5: a doc that diverges from the code it describes is an incident, and one of these was introduced by my own pin-enforcement fix #115). Also reflect the now-current engine configuration.

**The three stale claims, corrected:**

1. **Wrong org pin.** The header's ACTIVE inventory named `vars.CHARLY_VERSION = v2026.263.2131` welding `plugin-review@v2026.263.1956`. The actual pin is `v2026.267.0045`, welding `plugin-review@v2026.266.1944` (verified: `gh variable list` + `charly show v2026.267.0045:scripts/host-command-plugins.txt`).
2. **A fallback that no longer exists.** The header described "the workflow's built-in fallback TAG (v2026.254.1902)". My own #115 **deleted** that fallback: `ensure-charly` now uses `TAG="$CHARLY_VERSION"` verbatim and fails loud (exit 3) when the pin is unset/invisible. The header now says so. (Two more `v2026.254.1902` comments inside the step were removed for the same reason.)
3. **"INERT" knobs that are active.** The header claimed the sampling/penalty/stop/prompt knobs are "ABSENT from the org pin's welded plugin — INERT until the pin moves". Verified with `strings` against the pinned bundle: `AI_REVIEW_TOP_P`, `_FREQUENCY_PENALTY`, `_PRESENCE_PENALTY`, `_STOP`, `_PROMPT_EXTRA`, `_POST_COMMENT` **each have a hit** — they are effective today.

**Engine configuration reflected.** The generation-bound narrative now states the truth: the runaway was fixed by deleting the tool loop (`plugin-review v2026.264.1425`), and the engine defaults to `reasoning_effort=high` + `max_tokens=262144`. The short-lived `low`/`65536` mitigation (also `visibility=private`, so invisible to all public repos) has been **deleted**; the gate runs at the engine default HIGH (measured on the fixed engine: small PR ~70 s, +370/-108 PR ~285 s, both PASS).

Comment-only changes plus the matching harness assertions; no step body's behaviour changes.

## How tested

**The workflow's own harness — 267 assertions / 17 scenarios, ALL PASS:**

```
$ python3 .github/tests/validator-gate-harness.py
...
ALL PASS: 267 assertions across 17 scenarios, run against the real workflow run: bodies (fakes for charly and gh).
```

The three assertions that pinned the OLD strings (the wrong pin name, the "INERT" claim) were **updated in the same change** (R5) and now assert the corrected claims.

**The `strings` evidence for claim 3** (executed against the org-pinned bundle):

```
$ for k in AI_REVIEW_TOP_P AI_REVIEW_FREQUENCY_PENALTY AI_REVIEW_PRESENCE_PENALTY AI_REVIEW_STOP AI_REVIEW_PROMPT_EXTRA AI_REVIEW_POST_COMMENT; do
    printf '%-30s %s\n' "$k" "$(strings <pinned plugin-review> | grep -c "$k")"; done
AI_REVIEW_TOP_P                1
AI_REVIEW_FREQUENCY_PENALTY    1
AI_REVIEW_PRESENCE_PENALTY     1
AI_REVIEW_STOP                 1
AI_REVIEW_PROMPT_EXTRA         1
AI_REVIEW_POST_COMMENT         1
```

**The pin evidence for claims 1–2:**

```
$ gh variable list --org opencharly | grep CHARLY_VERSION
CHARLY_VERSION  v2026.267.0045  ...  ALL
$ charly show v2026.267.0045:scripts/host-command-plugins.txt | grep review
plugin-review@v2026.266.1944
$ grep -c 'TAG="${CHARLY_VERSION:-' .github/workflows/pr-validator.yml
0        # no fallback remains
```

## T4 — guardrail change, operator-authorized

This edits `.github/workflows/pr-validator.yml`, the org-wide merge guardrail (T4). The change is **comment-only** (no step body changes behaviour) and corrects the guardrail's own documentation to match its code — it neither weakens nor strengthens the gate mechanically. Operator direction recorded: run the validator locally and admin-merge on PASS.

## Rulebook compliance

- **R0** — loaded `/charly-internals:git-workflow`, `/charly-internals:strict-policy`.
- **R1** — three doc-divergences in the guardrail are RCA'd with executed evidence (`gh variable`, `strings`, `grep`) and corrected; one was introduced by my own #115.
- **R2** — all three fixed here; nothing deferred.
- **R3** — comment + matching harness assertions; no duplication.
- **R4** — no workaround.
- **R5** — the stale strings are deleted, and the harness assertions that pinned them are swept in the same commit.
- **R6** — `fix/validator-header-current-pin` off `origin/main`; no force-push.
- **R7 / R7a** — the gate's own harness runs green (267 assertions) and asserts the corrected claims.
- **R8 / R9** — workflow header + harness test only.
- **R10** — N/A (workflow comments + harness assertions; no bed/deploy/fresh-rebuild path executes).

## Change classification

- **Change class:** CI workflow-header documentation correction + matching harness assertions.
- **Verification gate:** the gate's own offline harness (267 assertions), with the corrected claims `strings`- and `gh variable`-verified.
- **Attribution tier:** `analysed on a live system` — the diff is documentation (workflow comments) PLUS executable harness code (changed `note(...)` conditions), so it is not the documentation-only class; the harness ran live (267 assertions) but the full validator gate did not pass.

**Cross-repo:** another open PR (opencharly/.github#114) edits the same file; ordering/merge coordination may be needed.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*