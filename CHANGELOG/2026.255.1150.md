# 2026.255.1150 — validator: fail-hard wall clock on the validate job (timeout-minutes: 20)

## Summary

Fail-hard wall clock + zero retries for the reusable `charly/pr-validator` gate (5 commits, 2 files, +49/−12):

1. `timeout-minutes: 20` on the `validate` job — GitHub cancels past the cap; the required check goes RED fast instead of hanging pending.
2. `AI_REVIEW_MAX_ATTEMPTS: 1` (default org-wide; any repo may opt back via its own var) — a failed first attempt is terminal, no repeat cycle. INERT until the engine release carrying the knob is welded and TAG is bumped (opencharly/plugin-review#5, engine PR opencharly/plugin-review#6); the pinned bundle ignores unknown env.
3. Attempt-timeout default `900` → `300` (the plugin's own built-in default, `review.go:28`): no recorded generation needing >5 min ever succeeded — the failures are generations that NEVER answer — so the cap is a deadline, not a budget. Stays org-overridable via repo vars.
4. Offline harness (`.github/tests/validator-gate-harness.py`) updated to the new intent: 900s pins → 300s, plus new wall-clock (`timeout-minutes: 20` present) and attempts (unset→1, set→value) coverage. Behavior pins move with the behavior, deliberately, in the same diff.
5. Harness hardening: the new attempts assertions bind their own empty namespace instead of reusing the preceding guard's binding (a missing TIMEOUT entry could otherwise surface as `NameError` instead of a clean failure).
6. Cap-justification comment aligned with the evidence: "observed REVIEWS 38s–13m19s" (the 13m19s run failed; even the slowest observed review fits inside 20) versus the single 43m54s provider-unanswered burn the cap exists to kill (run `34688719317`, classified INCONCLUSIVE) — the comment no longer says "observed runs" while the paste shows a 43m54s run.

Honest worst-case accounting (correcting v2's headline): **this PR as merged delivers 15 min worst case** (3×300s while the pinned bundle still retries) with the 20-min outer as backstop; **~5–6 min once the knob activates** post-release + TAG bump. The INCONCLUSIVE echo is now knob-aware ("up to AI_REVIEW_MAX_ATTEMPTS times") instead of hardcoding "3 times".

PASS/BLOCK/INCONCLUSIVE/AMBIGUOUS classification, fail-closed invariants, and auto-merge arming are untouched: a cancelled or single-failed run carries no verdict and can only red, never pass.

## How tested

YAML validation, real command + output (author clone at the PR head):

```
$ python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/pr-validator.yml')); j=d['jobs']['validate']; print('YAML OK; timeout-minutes:', j['timeout-minutes'])"
YAML OK; timeout-minutes: 20
```

Offline harness — the exact gate that failed on the previous head — real command + output (local, same stdlib harness CI runs):

```
$ python3 .github/tests/validator-gate-harness.py
exit=0, 107 PASS lines, zero FAIL (scenarios: 8, assertions: 107)
```

It failed pre-fix with exactly 2 FAILs (900s pins vs the new 300s behavior); post-fix 107/107 green, including the 2 new fail-fast assertions.

Gate-load evidence, real command + output (live `gh` against the org):

```
$ gh run list --limit 6 --json databaseId,status,conclusion,name,headBranch --jq '.[] | "\(.databaseId) \(.status)/\(.conclusion) \(.name) \(.headBranch)"'
34688719317 in_progress/ charly/pr-validator feat/opencode-marketplace-skills
34687106175 completed/skipped tag-on-merge main
34686543034 completed/failure charly/pr-validator sync/20260912-075857
34682156080 completed/skipped tag-on-merge main
34682099410 completed/failure charly/pr-validator sync/20260912-075857
34681846354 completed/success sync main
```

Observed runs conclude in 38s–13m19s; the 13m19s run FAILED — even the slowest observed run, failure included, fits inside 20 with margin, so 20 ≈ 1.5x the slowest review ever measured. Durations paste (default `gh run list` format carries elapsed time — the `--json` paste above cannot show it):

```
$ gh run list --limit 15
completed	skipped	tag-on-merge	tag-on-merge	main	workflow_run	34690619614	7s	2026-09-12T11:16:25Z
completed	failure	feat: auto-load marketplace skills in opencode config	charly/pr-validator	feat/opencode-marketplace-skills	pull_request	34688719317	43m54s	2026-09-12T10:32:29Z
completed	skipped	tag-on-merge	tag-on-merge	main	workflow_run	34687106175	1s	2026-09-12T09:55:41Z
completed	failure	chore: sync gitlinks	charly/pr-validator	sync/20260912-075857	pull_request	34686543034	13m19s	2026-09-12T09:42:20Z
completed	skipped	tag-on-merge	tag-on-merge	main	workflow_run	34682156080	7s	2026-09-12T08:00:25Z
completed	failure	chore: sync gitlinks	charly/pr-validator	sync/20260912-075857	pull_request	34682099410	1m16s	2026-09-12T07:59:05Z
completed	success	sync	sync	main	schedule	34681846354	6m32s	2026-09-12T07:52:38Z
completed	success	docs: add CHANGELOG 2026.254.1823 for PR #197	tag-on-merge	main	push	34632986949	24s	2026-09-11T18:23:25Z
completed	success	tag-on-merge	tag-on-merge	main	workflow_run	34632955288	28s	2026-09-11T18:23:04Z
completed	skipped	tag-on-merge	tag-on-merge	main	workflow_run	34632088695	1s	2026-09-11T18:13:42Z
completed	success	chore(umbrella): sync the eval-omarchy gitlink to the V4.1 lane pin HEAD	charly/pr-validator	feat/sync-gitlinks-eval-omarchy	pull_request	34631978111	38s	2026-09-11T18:22:24Z
completed	success	tag-on-merge	tag-on-merge	main	workflow_run	34623741915	44s	2026-09-11T16:44:39Z
completed	success	chore(umbrella): sync docs gitlink to the post-#118 corpus-sync main (e6f8148e)	charly/pr-validator	feat/sync-docs-corpus	pull_request	34623620535	1m17s	2026-09-11T16:43:20Z
completed	skipped	tag-on-merge	tag-on-merge	main	workflow_run	34623465875	1s	2026-09-11T16:41:43Z
```

Reading it: validator reviews complete in 38s–13m19s (the 43m54s row is run `34688719317`'s provider-unanswered burn, classified INCONCLUSIVE in that PR's thread — the failure mode this PR kills, not a review).

R1 on the pasted failures: the two `failure` runs above (`34686543034`, `34682099410`) are PR #198's sync-evidence BLOCKs (disjoint paste, missing tier/compliance sections, unverifiable `dotgithub` bump — read in full in that PR's thread), a body-evidence class distinct from the provider-unanswered burn this PR addresses. No `flake / transient` framing: named runs, named findings, separate defects.

N/A — workflow config, no runtime target: no disposable bed, no fresh rebuild, no binary exercise applies to a YAML-only gate change. The timeout path itself cannot execute live (it only fires on a stuck provider), so live execution caps at the evidence above.

Post-commit check: `git show --stat HEAD` lists exactly `.github/workflows/pr-validator.yml | 9 +++++----`; `git status --short` clean; push fast-forwarded append-only (no force). Cumulative branch diff: 2 files, +49/−12 over `main`.

## Rulebook compliance

- T4 (self-modifying guardrail): maintainer sign-off recorded on this PR (comment by @atrawog, 2026-09-12T11:32Z, updated to head `b98ff10`). The agent authored but did not land; the merge follows the green required check. No admin/force bypass.
- R0: `/charly-internals:git-workflow` (append-only review rounds, freeze-head-then-write discipline) and the failure evidence behind the change; `timeout-minutes` semantics verified against the GitHub workflow-syntax reference (job cancelled at cap, default 360), not assumed.
- R1: the round-2 verdict's three new blocks are each fixed at their root (stat recomputed cumulatively, worst-case restated with the INERT qualifier honored, 13m19s reconciled in the comment itself) — plus the second red check (`validator-harness`) root-caused to its 900s pins and updated, proven by the local 107/107 run above.
- R3: one wall clock (job level), one attempt knob (engine env, wired once) — no parallel timeout mechanisms.
- R4: no in-job retry added or implied; re-run is explicit (`gh workflow run pr-validator.yml -f pr-number=<N>`).
- R5: every `900s`-default claim swept in the same diff — workflow comments, INCONCLUSIVE echo, and harness pins. Sweep executed, pasted verbatim (only CHANGELOG history survives, which is immutable record, not live claim):

```
$ grep -rn "900s" --include="*.yml" --include="*.py" --include="*.md" . | grep -v "^\./\.git/"
./CHANGELOG/2026.255.1029.md:69:  - scenario provider-unanswered: comment body contains '900s default'
```
- R6: `feat/` branch, status checked, no force-push, no direct push to `main`, PR-only.
- R2 (named follow-ups): attempt-loop engine work is opencharly/plugin-review#5 (issue) + opencharly/plugin-review#6 (engine PR); activation needs a charly release weld + TAG bump as a third step. Per-turn streaming/bounding stays with the engine owner per the file's own header.

## Change classification

- Change class: CI/workflow config + its offline test pins (`.github/workflows/pr-validator.yml`, `.github/tests/validator-gate-harness.py`; no review logic, classification, permission, or auto-merge changes).
- Verification gate: `yaml.safe_load` + local `validator-gate-harness.py` 107/107 + live `gh` evidence (all pasted) + this PR's own validator run (self-gating repo).
- Attribution tier: analysed on a live system (YAML/harness/`gh` evidence ran live with retained output; the 20-min cancellation path itself cannot execute live — that is the ceiling).

*Assisted-by: opencode opencode-go/muse-spark-1.3-contributor (analysed on a live system)*