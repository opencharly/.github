# 2026.234.0111 — Sweep-only tag-on-merge
<!-- auto-merge-finalized: 2026.234.0111 -->

The head-based event lookup missed tags (the API rename produces
multiple commits, so the workflow_run head_sha does not match the
final PR head). Every validator completion now runs the 24h sweep —
robust by construction — with the schedule as redundancy.