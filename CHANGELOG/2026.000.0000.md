# 2026.000.0000 — Acceptance test: bot-token rename flow

This placeholder has an OLD stamp — the rename MUST happen. This PR
proves the full chain: rename (bot-token push) → synchronize run →
AI validation → auto-merge, all automatic.
