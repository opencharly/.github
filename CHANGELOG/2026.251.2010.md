# 2026.251.2010 — gate: pass GH_TOKEN to the ensure-charly bootstrap (live smoke fix)

## Summary

The new gate's first live run (smoke PR opencharly/action-review#3) failed at the Ensure charly bootstrap with exit 4: `gh: To use GitHub CLI in a GitHub Actions workflow, set the GH_TOKEN environment variable` — the step env carried only `CHARLY_VERSION`. Fix: pass `GH_TOKEN: ${{ github.token }}` explicitly. (Clean branch from current main — the previous attempt reused a merged branch head and conflicted with the cutover squash.)

## How tested

The failure was observed live on the smoke run. `actionlint` clean (including shellcheck). After this merges, the smoke PR is re-triggered and the gate runs end-to-end live: bootstrap → `charly review --plan` → verdict → comment → gate.

## Rulebook compliance

- R1: RCA'd from the run log; one env line.
- R3: single bootstrap step owns the gh calls.
- R5: clean cutover — the merged gate is the only validator path; this is a one-line hard-fail fix on it.

## Change classification

- Change class: hook/workflow (one env line).
- Verification gate: live re-run of the smoke PR after merge.
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*