# 2026.233.1615 — Auto-approve validator runs; remove dispatch step
<!-- auto-merge-finalized: 2026.233.1615 -->

GITHUB_TOKEN pushes (CHANGELOG rename) create pull_request:synchronize
runs in approval-required state. A new approve-validator-runs workflow
automatically approves them, so the validator re-runs on the PR head
without manual intervention. The dispatch step is removed (no longer
needed).

Change class: CI/workflow — auto-approve + dispatch removal.
