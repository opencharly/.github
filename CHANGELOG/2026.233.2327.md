# 2026.233.2327 — Fix tag-on-merge: workflow_call inputs+secrets fails to load
<!-- auto-merge-finalized: 2026.233.2327 -->

`workflow_call` declaring BOTH `inputs:` and `secrets:` is rejected at
workflow load time (caller runs fail with no jobs; isolated via
branch-ref caller tests: inputs-only loads, secrets-only loads, the
combination fails). The reusable now declares no secrets and relies on
the caller's `secrets: inherit`, matching pr-validator.yml. This
restores the per-merge CalVer tags that have been missing since the
dispatcher was deployed.