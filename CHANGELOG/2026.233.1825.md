# 2026.233.1825 — Rename-then-dispatch validation flow
<!-- auto-merge-finalized: 2026.233.1825 -->

The validator renames the CHANGELOG first, then dispatches a
validation run on the PR branch via workflow_dispatch (always runs,
no approval). The dispatched run's check lands on the PR head,
satisfying branch protection. Fix pushes trigger synchronize normally.

Change class: CI/workflow — flow redesign.
