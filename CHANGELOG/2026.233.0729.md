# 2026.233.0729 — Fix YAML parsing error in auto-merge-reusable.yml

The  expression inside a double-quoted
YAML string caused the colon after "Skip" to be interpreted as a mapping
separator in a flow context, breaking YAML parsing of the reusable
workflow. Fixed by escaping via single quotes.

Change class: CI/workflow — one character change.
