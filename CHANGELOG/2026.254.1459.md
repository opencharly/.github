# 2026.254.1459 — docs: align the AI_REVIEW_* variable documentation with the live gate

## Summary

Docs-only sweep of the org `.github` README to match the LIVE gate, as part of
the org-wide DeepSeek V4.0→V4.1 cutover:

- **`Required org-level configuration` table** now documents the real org
  variables: `AI_REVIEW_PROVIDER=ollama`, `AI_REVIEW_BASE_URL=https://ollama.com/v1`,
  `AI_REVIEW_MODEL=deepseek-v4.1-flash` (was the archived-era `openrouter` /
  `https://openrouter.ai/api/v1` / `~deepseek/deepseek-v4-flash-latest`).
- **Gate mechanism prose** corrected to the charly-native gate
  (`charly review --plan review-plan.yml`, plugin-review welded into the charly
  release) — not the archived `opencharly/pi-review-action`.
- **Scope & evidence baseline** fixed: `get_ci_status` removed (four tools only),
  the `api_key` env is restored in the prose (`provider/model/base_url/api_key`),
  and the stale `/CI state` phrase is gone.
- **Version-surface sentence** corrected (validator finding 4): the plan+prompt
  source (`opencharly/action-review@main`) is the validator spec's single
  config channel — deliberately mutable, updating without touching the workflow;
  the gate-mechanism version surface is the PINNED charly release
  (`vars.CHARLY_VERSION`; workflow default `v2026.251.1947`, bumped per release).
- Fixed a duplicated sentence in the org-level configuration paragraph.

No workflow YAML change.

## How tested

Docs-only change (whole diff is `README.md`). Gate: non-runtime standards —
sweep evidence + consistency against the live workflow and org vars.

1. **Residual sweep, executed (covers the removed capability too):**

```
$ grep -rn -i "pi-review-action\|openrouter\|v4-flash-latest\|get_ci_status\|CI-state" README.md
RC=1   # no matches
```

2. **Documented values == live org env** (verified 2026-09-11):

```
$ gh variable list --org opencharly
AI_REVIEW_BASE_URL  https://ollama.com/v1
AI_REVIEW_MODEL     deepseek-v4.1-flash
AI_REVIEW_PROVIDER  ollama
```

3. **Documented mechanism == checked-in workflow**: `dotgithub/.github/workflows/pr-validator.yml`
  runs `charly review --plan` with the `AI_REVIEW_*` env and the
  opencharly/action-review@main checkout (steps `[pr] → [ensure-charly] → [review]
  → [parse]`); the plan path is interpreted after `cd runner-config`, matching the
  documented `charly review --plan review-plan.yml`.

## Rulebook compliance

- **R0** — `git-workflow` skill; feat/-branch, PR-only, no direct push.
- **R1** — the anomalies this PR removes (stale CI-state claim, archived-action
  claims, `api_key` divergence) were found and fixed in-tree in the SAME PR.
- **R2** — no pre-existing split: every stale claim found is removed here; no
  follow-up framed.
- **R3** — no duplication: the variable table is the single doc; the gate has no
  second description.
- **R4** — no workarounds (no sleeps/retries/magic numbers in the diff).
- **R4a** — the documented command is the workflow's ACTUAL invocation: the
  workflow runs `cd "$GITHUB_WORKSPACE/runner-config"` then `charly review
  --plan review-plan.yml` — a reader executing exactly that works with the
  charly binary alone (no repo-relative path outside the documented checkout).
- **R5** — hard sweep: stale claims removed in the same commit; the executed grep
  above returns no residual.
- **R6** — `git status` clean before commit; no force-push.
- **R7** — docs-only class: the "gate" is the sweep evidence + the two
  consistency checks pasted above.
- **R8/R9/R10** — N/A (no generation, no artifacts, no runtime path changed).

## Change classification

- **Change class:** docs-only (`README.md`, 1 file, 1 commit family).
- **Verification gate:** non-runtime standards (sweep + consistency checks).
- **Attribution tier:** `documentation reviewed` — the whole diff is documentation;
  values cross-checked live against the org env and the checked-in workflow.

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (documentation reviewed)*