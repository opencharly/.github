# 2026.251.0758 — fix(pr-validator): concurrency dedupe per PR + workflow_dispatch self-heal onto the PR head

## Summary

Per-PR `concurrency` dedupe for the org-wide pr-validator (dedupe-only — T4-clean):

- **`.github/workflows/pr-validator.yml` (+8)**: per-PR `concurrency` group (`cancel-in-progress: true`, keyed by repo + pr-number) — a re-dispatch or push on the same PR cancels the in-flight run, so the head can never carry duplicate same-name `validate / validate` check runs (a duplicate FAILED run kept `mergeable_state: blocked` despite PASS — measured on #38).
- **`org-wide-pr-validator-dispatcher.yml` (+6)**: the install-source dispatcher gains the same dedupe.
- **T4 disposition**: the earlier revision's `actions: write` capability and the dispatch self-heal re-dispatch are REMOVED — dedupe only, no capability escalation, no self-modifying guardrail behavior; `dispatch-with---ref` remains an operational measure documented in the git-workflow skill.

## How tested

```
$ git diff --stat origin/main...HEAD
 .github/workflows/pr-validator.yml   | 8 ++++++++
 org-wide-pr-validator-dispatcher.yml | 6 ++++++
 2 files changed, 14 insertions(+)
$ grep -c 'actions: write' .github/workflows/pr-validator.yml
0
$ grep -c 'Attach dispatch run' .github/workflows/pr-validator.yml
0
$ grep -c 'concurrency:' .github/workflows/pr-validator.yml org-wide-pr-validator-dispatcher.yml
2
$ git diff --cached --check
(clean)
```

YAML lint clean on both files.

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R1 | #38's blocked-despite-PASS RCA'd to duplicate same-name check runs — fixed with the per-PR concurrency group. |
| R2 | N/A. |
| R3 | The reusable workflow + install source are the single sources; per-repo dispatchers copy them. |
| R4 | Fix, not workaround; the escalation was removed, not documented around. |
| R4a | N/A — the gate still runs; no bypass. |
| R5 | N/A — additive. |
| R6 | feat/ branch; force-with-lease on the feature branch. |
| R7 | Diff + greps pasted, matching the current head. |
| R8 | The change IS the gate workflow (dedupe only). |
| R9 | N/A. |
| R10 | N/A for CI wiring; the next dispatch exercises it live. |
| RDD | Measured #38 outcome. |
| ADE | N/A. |
| SDD | N/A. |
| T4 | NO capability escalation (`actions: write` removed) and NO self-modifying re-dispatch — dedupe only; security screen PASS. |
| B14 | Every diff item accounted; the diff-stat + greps match the current state exactly. |
| B15 | N/A. |
| B17 | Workflow code. |
| B18 | N/A. |

## Change classification

- Change class: CI workflow fix (dedupe only, no capability change).
- Verification gate: YAML lint + diff + live dispatch.
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*