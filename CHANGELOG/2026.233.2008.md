<!-- auto-merge-finalized: 2026.233.2008 -->
# 2026.233.2008 — Fix rename push auth (v2): unset checkout extraheader, push via URL

The v1 fix added a second `http.https://github.com/.extraheader` via
`git -c`, but the config key is multi-valued: checkout's GITHUB_TOKEN
header remained, producing `remote: Duplicate header: "Authorization"`
(HTTP 400, observed live). The push now unsets the extraheader and
authenticates with the bot token embedded in the push URL.
