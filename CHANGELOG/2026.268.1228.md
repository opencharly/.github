# 2026.268.1228 — docs(task): rewire the PR-template Taskfile-era task references

## Summary

Sweep the last stale Taskfile-era task references from the org PR template after
the umbrellas/charly Taskfile→`charly task` cutover. R5 stale-reference sweep;
documentation-only.

- `.github/PULL_REQUEST_TEMPLATE.md` R9 "Fresh rebuild" now names
  `./scripts/bootstrap-charly.sh` (the ONE non-charly bootstrap build) instead of
  the deleted `task build:binary`;
- the RDD/ADE/SDD checklist line and the glossary's SDD entry now name
  `charly task cue-gen` instead of the deleted `task cue:gen`.

Historical mentions in `CHANGELOG/` are left intact.

## How tested

The claim-keyed sweep returns no stale reference on the final tree:

```
$ grep -rnE '\btask (build:binary|cue:gen|wire:gen|proto:gen|verify|sync|mods:tidy|build:install-portable|setup:all)\b|Taskfile' . | grep -v '^./CHANGELOG/'
(empty)
```

The replacements were traced to their deleted surfaces before being written:
`task build:binary` was replaced by `scripts/bootstrap-charly.sh` in charly#651,
and `task cue:gen` is now the spec repo's `cue-gen` task (which this change
names in full as `charly task cue-gen`).

## Rulebook compliance

- **R5 (delete legacy completely):** every stale reference in the template is
  updated in the same commit; only `CHANGELOG/` keeps the old names as history.
- **The replacement claim carries the removal's burden of proof:** both
  replacements were traced to the commits/tasks that superseded the old names.
- **Documentation-only change class.**

## Change classification

- **Change class:** documentation-only (template prose; zero behavior change).
- **Verification gate:** claim-keyed R5 sweep empty.
- **Attribution tier:** `documentation reviewed`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*