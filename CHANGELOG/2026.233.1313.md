# 2026.233.1313 — Bump pi-review-action to v1.0.8 (retry + diagnostic logging)

v1.0.8 adds up to 3 retry attempts with backoff when the model returns
an empty message or a response without a Verdict line, plus diagnostic
logging for every model call. This makes transient model failures
self-healing.

Change class: CI/workflow — action version bump.
