# 2026.233.1600 — Dispatch validator re-run to default branch

The dispatch step targeted the PR head_ref, which may have an older
version of the reusable workflow (e.g. before a pi-review-action bump).
Dispatching to the default branch ensures the latest reusable workflow
is used.

Change class: CI/workflow — dispatch ref fix.
