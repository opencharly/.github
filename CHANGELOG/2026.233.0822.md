# 2026.233.0822 — Add workflow_run trigger alongside check_run

Adds the workflow_run trigger back to the auto-merge dispatcher as a
parallel trigger to check_run.  Both triggers include a branch guard
(head_branch != default_branch) to prevent firing on the default branch.

Change class: CI/workflow — trigger configuration change.
