# 2026.233.1231 — Add auto-retry to pr-validator for empty responses

Adds up to 3 automatic retry attempts when the AI review returns an
empty response. Each attempt runs the pi-review-action independently.
After 3 empty responses, the workflow exits with an infra error.

Change class: CI/workflow — pr-validator retry logic.
