# 2026.234.0129 — Use merged_at in the tag sweep
<!-- auto-merge-finalized: 2026.234.0129 -->

The pulls LIST endpoint does not populate `merged` (it is null in the
list response); `merged_at` is the reliable field. Without it, every
PR was skipped by the sweep (observed: "Sweep complete" with no
"Tagged" lines).