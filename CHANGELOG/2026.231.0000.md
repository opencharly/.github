# 2026.231.0000 — placeholder CalVer (finalized by the pr-validator at merge)

## Added — org-wide `charly/pr-validator` workflow

- **`.github/workflows/pr-validator.yml`** — replaced the manual, agent-posted
  `charly/pr-validator` commit status with an actual GitHub Actions workflow that
  runs the [`shaftoe/pi-coding-agent-action`](https://github.com/shaftoe/pi-coding-agent-action)
  as a fresh, independent PR validator on every non-fork pull request in the
  org. The job is named `charly/pr-validator`, so its check run satisfies the
  org-wide branch-protection required context of that name.
- **Fully generic, nothing hardcoded.** `provider`/`model`/`base_url` come from
  org variables and the API key from the `AI_REVIEW_API_KEY` org secret; pi
  resolves the model in its built-in provider catalog, sets the key via
  `setRuntimeApiKey()`, and overrides the endpoint via `registerProvider()`. No
  `models.json` is written.
- **Single comment, no duplication.** The action natively posts the agent's
  response (success and failure); no separate `gh pr comment` step.
- **Gate.** The verdict step (`exit 0` PASS / `exit 1` BLOCK / `exit 2`
  infra-or-no-verdict) always runs so the check state is set even on infra
  failure.
- **Strict PR-template compliance** is a hard gate; **`Assisted-by`** is
  deterministic (`Assisted-by: Nobody` for human-only work).
- **`README.md`** — documented the workflow and its required org-level config.

## Configuration

Org-level (provided out-of-band, not committed):

- `AI_REVIEW_PROVIDER` = `openrouter`
- `AI_REVIEW_BASE_URL` = `https://openrouter.ai/api/v1`
- `AI_REVIEW_MODEL` = `~deepseek/deepseek-v4-flash-latest`
- `AI_REVIEW_API_KEY` = provider API key (org secret)
# 2026.231.0000 — placeholder CalVer (finalized by the pr-validator at merge)

## Added — org-wide `charly/pr-validator` workflow

- **`.github/workflows/pr-validator.yml`** — replaced the manual, agent-posted
  `charly/pr-validator` commit status with an actual GitHub Actions workflow that
  runs the [`shaftoe/pi-coding-agent-action`](https://github.com/shaftoe/pi-coding-agent-action)
  as a fresh, independent PR validator on every non-fork pull request in the
  org. The job is named `charly/pr-validator`, so its check run satisfies the
  org-wide branch-protection required context of that name.
- **Fully generic, nothing hardcoded.** `provider`/`model`/`base_url` come from
  org variables and the API key from the `AI_REVIEW_API_KEY` org secret; pi
  resolves the model in its built-in provider catalog, sets the key via
  `setRuntimeApiKey()`, and overrides the endpoint via `registerProvider()`. No
  `models.json` is written.
- **Single comment, no duplication.** The action natively posts the agent's
  response (success and failure); no separate `gh pr comment` step.
- **Gate.** The verdict step (`exit 0` PASS / `exit 1` BLOCK / `exit 2`
  infra-or-no-verdict) always runs so the check state is set even on infra
  failure.
- **Strict PR-template compliance** is a hard gate; **`Assisted-by`** is
  deterministic (`Assisted-by: Nobody` for human-only work).
- **`README.md`** — documented the workflow and its required org-level config.

## Configuration

Org-level (provided out-of-band, not committed):

- `AI_REVIEW_PROVIDER` = `openrouter`
- `AI_REVIEW_BASE_URL` = `https://openrouter.ai/api/v1`
- `AI_REVIEW_MODEL` = `~deepseek/deepseek-v4-flash-latest`
- `AI_REVIEW_API_KEY` = provider API key (org secret)


- **Static evidence baseline (honest capability scope).** The validator runs with no
  shell (four tools: `get_pr_diff`, `get_issue_or_pr_thread`, `get_ci_status`,
  `get_workflow_run_logs`). It does not re-execute runtime beds, generators, or
  greps; it cross-checks pasted evidence statically with explicit tool-limited
  dispositions. Deep independent re-execution of runtime-class evidence remains the
  shell-enabled fresh-evaluator agent's job in the `charly` repo. The org baseline
  for runtime-class PRs reviewed by this gate is static cross-check, not live re-run
  (see README "Scope & evidence baseline").
- **Structural merge-order gate.** The gate's PASS path stays red until the companion
  cut-over (`opencharly/charly` #349, `opencharly/plugins` #213) has actually merged
  (removing the agent-side posting), so there is never a dual writer to the
  required `charly/pr-validator` context. Permanent green after the companions land.
