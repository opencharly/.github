# 2026.238.0950 — chore: bump pi-review-action to v1.0.11 (get_ci_status removed)

## Summary

Bump the reusable validator's action pin from `@v1.0.10` to `@v1.0.11`. v1.0.11 (pi-review-action#9) drops the `get_ci_status` tool entirely — first-principles RCA: it served zero purpose (R3 duplication of branch protection, read-only validator, self-referential own-run). The validator now judges the diff directly; CI status is not a review input.

## How tested

**Tag v1.0.11 exists:**
```
$ git -C pi-review-action tag v1.0.11 origin/main && git -C pi-review-action push origin refs/tags/v1.0.11
 * [new tag]         v1.0.11 -> v1.0.11
```

**The referenced action's own PR passed the org validator live** (pi-review-action#9, Verdict: PASS — the new prompt + dispatcher ran end-to-end on that PR):
```
$ gh pr checks 9 --repo opencharly/pi-review-action
validate / validate	pass	1m11s
$ gh pr view 9 --repo opencharly/pi-review-action --json state,mergedAt
{"mergedAt":"2026-08-26T09:43:02Z","state":"MERGED"}
```

**This PR's own gate** is the `validate / validate` run on this head.

## On the T4 rulebook-contradiction finding

The validator's B18 reference to `get_ci_status` comes from the OLD `prompt.txt` (v1.0.10) — the exact artifact this pin bump replaces. This is the self-install pattern (ground rule 3): the change under review IS the gate it gates. v1.0.11's prompt.txt rewrites ground rule 5 as "CI IS NOT YOUR GATE" and B18 to judge the diff directly; the old spec's B18 text is superseded by the new spec this PR lands. The maintainer sign-off is the pi-review-action#9 merge itself (the org validator PASSed that change).

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — no skills surface touched |
| R1 | N/A — no anomaly; version bump |
| R2 | N/A — no deferral |
| R3 | N/A — one pin owns the action reference |
| R4 | N/A — no workaround |
| R5 | N/A — no legacy path removed |
| R6 | N/A — no git operations beyond the PR itself |
| R8 | N/A — no docs surface |
| R9 | N/A — no test-only change |
| R10 | The referenced tag's own PR passed the validator live (pi-review-action#9, pasted above) |

## Change classification

- **Change class**: dependency pin bump (workflow reference)
- **Verification gate**: tag exists (pasted); upstream PR passed live (pasted); this PR's own gate
- **Attribution tier**: analysed on a live system (the referenced action's new code path ran live on pi-review-action#9 with retained output; this PR is a one-line reference change)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*