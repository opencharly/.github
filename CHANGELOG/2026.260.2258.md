# 2026.260.2258 — docs(template): the model-free generator body form

## Summary of changes

Mirror of the merged A1 amendment (`opencharly/action-review#6`) into the org PR template. The
attribution comment block stated only the AI-authored form; it now also documents the MODEL-FREE
`<Harness> <Runtime>` form a fixed CI generator's body must use (e.g.
`*Assisted-by: GitHub Actions ubuntu-latest (fully tested and validated)*`), and forbids a
fabricated AI model name and an `N/A` placeholder.

**Closes / relates to:** mirrors `opencharly/action-review#6` (merged).

**Breaking change + rollback:** none — a comment-block addition; revert restores the prior text.

**Files in the diff** (`git diff --stat origin/main...HEAD`):
```
 .github/PULL_REQUEST_TEMPLATE.md | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)
```

## Change class

**Harness project configuration** (PR-template prose).

## How R10-tested — exact gate, commands, and pasted output

- **The gate:** the non-runtime standards (template prose).
- **Eval beds run:** `N/A — template prose`.
- **Fresh rebuild:** `N/A — no binary`.
- **Did the CHANGED code path execute live?** `N/A — no code path`.

**Evidence — put each item in the block whose label names it.**

*1. Fresh-rebuild gate output:* `N/A`.
*2. Each changed code path executing:* `N/A`.
*3. Docs-only evidence:*
```
$ git diff --stat origin/main...HEAD
 .github/PULL_REQUEST_TEMPLATE.md | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)
```

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** `/charly-internals:skills` (mirroring surfaces) — the template is a mirroring
  surface for the attribution rule.
- **R1 RCA + zero warnings:** RCA'd to the A1 amendment not being mirrored into the template.
- **R2 no out-of-scope:** same theme; the rulebook/skill/agent-spec halves landed in their repos.
- **R3 no duplication:** the mandate lives in the rulebooks; the template states the form once.
- **R4 no workaround:** the template now matches the rulebook the gate enforces.
- **R5 hard cutover + grep:** the claim-keyed sweep of the AI-only wording is applied across the
  mirroring surfaces.
- **R6 git safety:** `git status` checked; no force-push.
- **R7 runtime gate:** `N/A`.
- **R8 artifact invariants:** `N/A`.
- **R9 binary == source:** `N/A`.
- **R10 disposable + coverage:** `N/A — documentation change class`.
- **RDD / ADE / SDD:** RDD — the amendment's premise was verified by the two blocked generator
  PRs. ADE — unchanged. SDD — `N/A`.
- **Hard cutover:** one atomic commit.
- **Kernel/plugin boundary law:** `N/A`.
- **Disposable-only autonomy:** `N/A`.
- **Clean architecture + Go gates:** `N/A — no Go`.
- **CHANGELOG:** this repo keeps `CHANGELOG/`; tag-on-merge writes the entry from this body.

## Attribution tier

`documentation reviewed` — the whole diff is template prose; the non-runtime standards pass.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*